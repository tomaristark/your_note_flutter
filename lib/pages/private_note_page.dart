
import 'package:flutter/material.dart';

class PrivateNotePage extends StatelessWidget {
  const PrivateNotePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}